<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
    <% if Helpers.current_user.is_logged_in? %>
    <p>Welcome <%=@user.username%></p>
    <p>Your balance is: <%= @user.balance %></p>
    <% else %>
    <p>You Must <a href='/'>Log In</a></p>
    <%end%>
    
    <a href="/logout">Log Out</a>

  </body>
</html>